spring:
  datasource:
    url: jdbc:mysql://localhost:3306/questiondb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true



myapp:
  msg: hello from configuration
